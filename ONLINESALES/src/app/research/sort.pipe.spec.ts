/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TriPipe } from './tri.pipe';

describe('TriPipe', () => {
  it('create an instance', () => {
    const pipe = new TriPipe();
    expect(pipe).toBeTruthy();
  });
});
